# 05_数据库连接与依赖配置

## 定义
后端通过驱动和 ORM 框架连接并操作数据库。

## 核心点
- 驱动：PostgreSQL。
- ORM：Spring Data JPA + Hibernate。
- 配置文件控制连接参数。

## 项目落地
- 相关依赖在 `backend/pom.xml`。

## 面试快问快答
- 问：为什么不是全手写 SQL？
- 答：当前项目规模下，JPA 开发效率更高。