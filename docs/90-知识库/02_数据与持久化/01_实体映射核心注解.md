# 01_实体映射核心注解

## 定义
JPA 注解用于把 Java 对象映射到数据库表。

## 核心点
- `@Entity`：实体。
- `@Id`、`@GeneratedValue`：主键。
- `@Column`：字段约束。
- `@Table`：表名映射。

## 项目落地
- `User`、`Todo` 都由 JPA 管理持久化。

## 面试快问快答
- 问：为什么用 JPA 注解映射？
- 答：减少样板代码，提高开发效率。