# 04_认证上下文与当前用户

## 定义
认证上下文用于保存当前请求的登录用户信息。

## 核心点
- token 校验通过后写入上下文。
- 业务层从上下文取当前用户。
- 不信任前端传入 userId。

## 项目落地
- `CurrentUserService.requireCurrentUser()`。

## 面试快问快答
- 问：为什么后端不能信任前端 userId？
- 答：可被伪造，存在越权风险。