# 05_Todo页面状态管理

## 定义
页面状态管理是组织加载、提交、筛选、弹窗等 UI 状态。

## 核心点
- 加载态和提交态分离。
- 列表筛选、搜索、排序分离。
- 异步请求后要在 `finally` 恢复状态。

## 项目落地
- `TodoView.vue` 使用 `ref/reactive/computed` 管理状态。

## 面试快问快答
- 问：为什么有时页面会一直 loading？
- 答：异常分支未正确重置状态。