# 05_DTO与实体分离

## 定义
DTO 服务接口层，实体服务持久化层。

## 核心点
- 避免实体直接暴露给外部。
- 降低接口和数据库结构耦合。
- 输入输出模型可独立演进。

## 项目落地
- 请求 DTO：`TodoCreateRequest`、`TodoUpdateRequest`。
- 响应 DTO：`TodoResponse`。

## 面试快问快答
- 问：为什么不直接返回实体？
- 答：可控性差，容易泄露内部字段。