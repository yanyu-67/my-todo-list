# 01_项目后端分层职责

## 定义
分层是按职责拆分代码，降低耦合。

## 核心点
- Controller：接收请求与返回响应。
- Service：承载业务规则。
- Repository：数据访问。
- 依赖方向应自上而下。

## 项目落地
- `TodoController` 不直接查库。
- `TodoService` 处理“只能操作自己的任务”。
- `TodoRepository` 提供按用户过滤查询。

## 面试快问快答
- 问：为什么不把逻辑全写在 Controller？
- 答：难维护、难测试、难复用。