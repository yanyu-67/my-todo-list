# 04_Optional在查询中的使用

## 定义
`Optional<T>` 表示查询结果可能为空。

## 核心点
- 用 `orElseThrow` 处理空值分支。
- 不建议直接 `get()`。
- 适合单条查询结果。

## 项目落地
- 用户查询与任务查询均用 `orElseThrow`。

## 面试快问快答
- 问：Optional 是否应作为实体字段？
- 答：通常不建议。