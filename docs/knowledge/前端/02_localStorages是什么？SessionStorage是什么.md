# localStorage是什么？

localStorage是浏览器中长期存数据的仓库，除非手动删，否则一直存在

常用来存：

- 登录状态（Token）
- 主题设置
- 语言偏好
- 购物车内容

# SessionStorage是什么

SessionStorage也是浏览器中的仓库，区别在于只存在于当前会话，关掉标签页，数据就没了

常用来存：

- 敏感但不需要长期保存的信息
- 表单填到一半的临时内容