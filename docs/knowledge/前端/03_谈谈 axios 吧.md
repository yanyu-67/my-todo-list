# 谈谈 axios 吧

axios是一个**发网络请求**的工具

<u>网络需要从服务器拿数据/提交数据，就用axios发请求</u>，（比如登录，提交表单，加载文章列表）

- 能够**自动转换数据**（服务器返回JSON，axios自动转成JS对象）
- **处理错误省心**，服务器返回404、500错误状态码，axios会自动当成错误，直接进catch
- **能统一配置**，eg: 给所有请求自动带上登录Token，统一设置服务器地址
- **拦截器**，可以在请求发出前、响应回来后**统一做点事**，eg: 自动加 token、统一处理错误

